# binding-concept-demo

Mini Angular app used in Step 1 to explain interpolation, property binding, event binding, and a preview of two-way binding.

## Run

```bash
npm install
npm run start
```

Open:
- `http://localhost:5300`
