# Foro - Cierre Clase 07

## ¿Que vimos hoy?
- Binding en Angular: `{{ }}`, `[prop]`, `(event)`.
- Flujo de eventos en componentes (`input`, `click`, `submit`).
- Login con captura de datos y validacion minima.
- Composer del chat reactivo (enviar + limpiar + feedback visual).
- Variante minima con `ReactiveFormsModule` y validadores basicos.

## Duda abierta para la proxima
¿Como modelarias conversaciones y mensajes en memoria para que el chat sea navegable sin backend?
