# c09-chat-app-end

Expected final snapshot for Class 09.
Includes:
- typed models in `core/model`,
- `MockBackendService` for local backend simulation,
- `AuthService` for local auth/session,
- `ChatService` for chat state and actions,
- lighter `LoginComponent` and `ChatComponent`.

## Run

```bash
npm install
npm run start
```
