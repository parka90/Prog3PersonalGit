# Static Layout Prototypes

This folder contains static design prototypes for the `UTNito` app.

## Files

- `login.html` + `login.css`
- `chat.html` + `chat.css`
- `tokens.css` (shared design tokens)
- `assets/utnito-logo.svg`

These layouts are the design baseline before Angular implementation updates.
