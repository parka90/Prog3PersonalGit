# c05-chat-app-end

## Espanol

Resultado final esperado de la Clase 05.

Incluye:
- Angular app con `LoginComponent` y `ChatComponent`.
- Migracion visual desde HTML/CSS estatico a templates Angular.
- Cambio de pantalla login/chat con estado local en `AppComponent`.

### Uso

```bash
npm install
npm run start
```

## English

Expected final result for Class 05.

Includes:
- Angular app with `LoginComponent` and `ChatComponent`.
- Visual migration from static HTML/CSS into Angular templates.
- Login/chat screen switch using local state in `AppComponent`.

### Usage

```bash
npm install
npm run start
```
