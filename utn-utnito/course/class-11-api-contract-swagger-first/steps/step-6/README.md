# Step 6 - Snapshot final de Clase 11

## Espanol

### Objetivo
Consolidar estado final de contrato API definido en clase.

### Archivos a copiar
- carpeta completa `src/` de este step (`steps/step-6/src`).
- (equivale al snapshot final de `c11-api-contract-end/src`).

### Ejecucion del step
- Copiar estos archivos sobre `c11-api-contract-start`.
- Levantar con `npm run start:dev` o con VS Code `Launch Nest Application` (`start:debug`).

### Conceptos
- Contrato completo de `auth`, `conversations`, `messages`.
- Swagger listo para usar como base de implementacion en clase 12.
- Introduccion de `responseBuilder` como refactor para evitar repetir estructura de respuesta.
