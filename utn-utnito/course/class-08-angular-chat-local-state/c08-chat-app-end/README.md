# c08-chat-app-end

Expected final snapshot for Class 08.
Includes:
- local state for conversations and messages,
- dynamic sidebar selection,
- local `New chat` flow,
- local send flow with mock reply,
- local archive action with `🗑` button,
- simple title filter for non-archived chats.

## Run

```bash
npm install
npm run start
```
