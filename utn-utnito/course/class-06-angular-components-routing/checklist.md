# Checklist - Clase 06

## Espanol

- [ ] `steps/step-1/routing-concept-demo` corre y permite navegar entre `/one` y `/two`.
- [ ] `c06-chat-app-start` corre con Angular.
- [ ] Existe `app.routes.ts` con rutas de login/chat.
- [ ] `AppComponent` usa `router-outlet`.
- [ ] Enviar login navega a `/chat`.
- [ ] Click en logout navega a `/login`.
- [ ] Ruta invalida redirige a `/login`.

## English

- [ ] `steps/step-1/routing-concept-demo` runs and navigates between `/one` and `/two`.
- [ ] `c06-chat-app-start` runs with Angular.
- [ ] `app.routes.ts` exists with login/chat routes.
- [ ] `AppComponent` uses `router-outlet`.
- [ ] Login submit navigates to `/chat`.
- [ ] Logout click navigates to `/login`.
- [ ] Invalid route redirects to `/login`.
