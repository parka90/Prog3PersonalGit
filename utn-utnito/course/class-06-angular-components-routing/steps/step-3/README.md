# Step 3 - Activar router-outlet en AppComponent

## Espanol

### Objetivo
Usar `router-outlet` como contenedor de pantallas por ruta.

### Archivos a copiar
- `src/app/app.component.ts`
- `src/app/app.component.html`

### Conceptos
- Componente raiz como host de rutas
- Reemplazo de `currentView` por URL

### Validacion
- Ir a `/login` muestra login.
- Ir a `/chat` muestra chat.

---

## English

### Objective
Use `router-outlet` as route view host.
