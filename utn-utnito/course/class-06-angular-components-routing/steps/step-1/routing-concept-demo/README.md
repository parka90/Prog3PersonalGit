# routing-concept-demo

Mini Angular app used only in Step 1 to explain routing basics.

This demo includes both navigation styles:
- UI navigation: `routerLink`
- Logic navigation: `router.navigate()`

## Run

```bash
npm install
npm run start
```

Open:
- `http://localhost:5300/one`
- `http://localhost:5300/two`
