# c06-chat-app-end

## Espanol

Resultado final esperado de Clase 06.

Incluye:
- Rutas `/login` y `/chat`.
- `router-outlet` en `AppComponent`.
- Login navega a chat.
- Logout navega a login.
- Redirects para ruta vacia y rutas invalidas.

### Uso

```bash
npm install
npm run start
```

## English

Expected final result for Class 06.

Includes:
- `/login` and `/chat` routes.
- `router-outlet` in `AppComponent`.
- Login navigates to chat.
- Logout navigates back to login.
- Redirects for empty and invalid routes.

### Usage

```bash
npm install
npm run start
```
