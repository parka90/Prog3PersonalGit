# c10-chat-app-start

Starting snapshot for Class 10.
This is equivalent to the final state from Class 9.

Includes:
- typed models in `core/model`,
- `MockBackendService`, `AuthService`, `ChatService`,
- login and chat already split by responsibilities,
- synchronous/local service flow (before async upgrade).

## Run

```bash
npm install
npm run start
```
