# async-concept-demo

Mini app for Class 10 Step 1.

Goal:
- show a simple async request flow,
- render loading/success/error states,
- practice `subscribe` + `finalize`.

## Run

```bash
npm install
npm run start
```
