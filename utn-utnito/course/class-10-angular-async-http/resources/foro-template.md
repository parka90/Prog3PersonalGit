# Foro - Cierre Clase 10

## Que vimos hoy?
- La estructura de clase 9 se mantiene (`model` + `service`).
- Agregamos `provideHttpClient()` y `ChatApiService`.
- Consumimos datos async desde `backend_mock/*.json`.
- Sumamos estados de UI: `loading`, `error`, `sending`.
- `ChatComponent` usa `subscribe` y `finalize`.

## Duda abierta para la proxima
Si hoy ya consumimos json mock via HTTP, que cambia cuando diseniemos el contrato real en Swagger (clase 11)?
