# Checklist - Clase 04

## Español

- `chat.html` carga `chat.js` con `defer`.
- Se captura `submit` con `preventDefault`.
- Step 3: demo mínima muestra mensajes en pantalla.
- Step 4: demo mínima agrega respuesta mock asincrónica.
- Step 5: demo de slider mueve ancho de panel izquierdo.
- Step 6: integración final permite:
  - crear chat,
  - cambiar chat activo,
  - eliminar chat con `X`,
  - enviar mensajes,
  - respuesta mock,
  - resizer lateral.
- Archivos finales listos:
  - `chat.html`
  - `chat.css`
  - `chat.js`

---

## English

- `chat.html` loads `chat.js` with `defer`.
- `submit` is captured with `preventDefault`.
- Step 3 minimal demo renders user messages.
- Step 4 minimal demo renders async mock reply.
- Step 5 slider demo updates left panel width.
- Step 6 final integration supports:
  - create chat,
  - switch active chat,
  - delete chat with `X`,
  - send messages,
  - mock reply,
  - sidebar resizer.
- Final files ready:
  - `chat.html`
  - `chat.css`
  - `chat.js`
