# c04-html-end

## Español

Resultado final esperado de la Clase 04.

Incluye:
- `chat.html` con script JavaScript conectado.
- `chat.js` con DOM, eventos, async mock y `fetch`.
- estado local de conversaciones (`new chat`, selección de chat activo, mensajes por conversación).
- resizer lateral para cambiar el ancho sidebar/chat desde JavaScript.
- `chat.css` con estilos para lista dinámica de chats y resizer.
- `login.html` simplificado, consistente con el end de Clase 2.

## English

Expected final result for Class 04.

Includes:
- `chat.html` with connected JavaScript.
- `chat.js` with DOM, events, async mock, and `fetch`.
- local conversation state (`new chat`, active chat selection, messages per conversation).
- drag resizer to modify sidebar/chat width with JavaScript.
- `chat.css` styles for dynamic chat list and resizer.
- simplified `login.html`, consistent with Class 2 end.
